<% @videos.each do |video| %>
<div>
  <%= link_to video_path(video.id) do %>
    <video src="<%= rails_blob_path(video.get_video) %>" type="video/mp4" controls></video>
  <% end %>
  <p>タイトル：<%= video.title %></p>
  <p>説明：<%= video.caption %></p>
  <p>ユーザーネーム：<%= video.user.name %></p>
</div>
<% end %>